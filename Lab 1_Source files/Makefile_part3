# Makefile - builds CS6015 Project
#
# To build a target, enter:
# make printM
#
# Targets:
# printM - See if these .o files exist, if not, do following documents(caps.o, clothing.o, etc.).
# Caps.o... - Execute the commands
# clean - clean all the file as commands told.
#
#Build all this if .o file are exists. Otherwise, do the following commands.
# $^ specify all .o file it mentions
# $@ Take the "printM" as output file name
printM: Caps.o Clothing.o main.o Pants.o
	c++ $^ -o $@

Caps.o: Caps.cpp Caps.h
	c++ -c $<

Clothing.o: Clothing.cpp Clothing.h
	c++ -c $<

Pants.o: Pants.cpp Pants.h
	c++ -c $<

main.o: main.cpp
	c++ -c $<

.PHONY: clean
clean:
	rm -f *.o *.out printM
	
